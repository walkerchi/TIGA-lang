# Tiga 项目记忆（原 GraphForge）

## Docs 风格规则（用户明确要求，2026-08-25）

### 示意图（SVG）风格：像人画的，不像 AI 生成的
- **重点唯一且明确**：人脑一次只能处理两三个信息。每张图只传达一个核心点，
  围绕它组织视觉层次（大小、粗细、颜色只突出这一处）。AI 生成图的典型毛病是
  把所有信息平铺、每个元素同等醒目，等于没有重点。
- **简要优先**：元素越少越好。能不画的就不画，能用一句话说的就不进图。
- **背景与页面配合**：图内背景色块的分割必须和页面标题/小节的分割对齐，
  不要出现和页面结构无关的交错底色，否则读者无法把图和正文对应起来。
- 改图后必须截图检查实际渲染效果再交付。

### 术语与语言
- 固定技术术语保留英文（reducer、kernel、tile、staged、halo、launch geometry、
  subclass、VJP、lowering、eager、oracle、pass、provider、backend、emitter、
  Domain/Iter/Kernel/Task IR 等）。
- 术语首次出现加超链接：英文术语链 Wikipedia，中文术语链百度百科。
- **文档不用第二人称**：不写 You/你/您的（doc 不是对话），用祈使句或被动/中性表述。

### 排版
- 公式一律 `$$` 独立成行居中，不和文字挤在一行；名称与公式之间不用破折号。
- 示例页的 `python examples/xxx.py` 命令必须带链接（GitHub 源码或对应文档锚点）。
